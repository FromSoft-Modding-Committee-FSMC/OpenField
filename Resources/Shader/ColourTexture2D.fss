#version 330 core
layout(location = 0) out vec4 FragDiffuse;

in vec4 fragColour;
in vec4 textureData;

uniform vec4 colour;
uniform sampler2DArray s2dTexture;

void main()
{
	FragDiffuse = (fragColour * colour) * texture(s2dTexture, textureData.xyz);
}